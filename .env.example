# GNews API key — used server-side only (api/gnews.js in prod, vite dev proxy locally).
# GNEWS_API_KEY is preferred; VITE_GNEWS_API_KEY still works for the existing Vercel env var.
GNEWS_API_KEY=your_gnews_api_key_here
